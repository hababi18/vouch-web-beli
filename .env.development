# Used automatically by `npm run dev` (Vite dev mode).
# Points the frontend at the API running locally on its own port.
# Not used for `npm run build` — see .env.example for why.
VITE_API_URL=http://localhost:4000
