import type { HubConfig } from '../types/vouch';

export const hubConfig: HubConfig = {
  channelName: 'Beli',
  handle: '@Conexion48',
  bio: 'Canal oficial de referencias y comprobantes de entrega. Aquí no vendemos, solo mostramos pruebas reales de nuestros clientes.',
  telegramUsername: 'conexion48',
  avatarUrl: '/logo.jpg',
  bannerNotice:
    '✅ Canal verificado · Todas las referencias son de compras reales',
};

export const TOTAL_VOUCHES_LABEL = '+150 Entregas Verificadas';
